@*@model List<EntityLayer.Entities.Product>
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<section>
    <div class="container">
        <div class="box-head" style="color: #428bca; text-transform: uppercase; font-size: 4rem; margin-bottom: 6rem; border-bottom: 0.1rem solid #428bca ">
            <h1>OYUNLAR</h1>
        </div>
        <div class="row">
            @Html.Action("CategoryList", "Category")
            <div class="col-sm-9 padding-right">
                <div class="features_items">
                    @foreach (var item in Model)
                    {
                        <div class="col-sm-4">
                            <div class="product-image-wrapper">
                                <div class="single-products">
                                    <div class="productinfo text-center">
                                        <a href="/Product/Details/@item.Id"><img src="~/Content/images/@item.Image" width="100" height="200" alt="" /></a>
                                        <h3>@item.Name</h3>
                                        <h2>@item.Price</h2>
                                        <a href="/Product/ProductDetails/@item.Id" class="btn btn-default add-to-cart">Detaylar için tıklayınız...</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
        @Html.Action("PopularProduct", "Product")
    </div>
</section>*@

